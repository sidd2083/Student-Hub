Fix the critical authentication, routing, and setup issues in my React + Firebase app "Student Hub".

CURRENT PROBLEMS (MUST FIX ALL):

1. On site load:
- It shows loading for too long
- Then wrongly redirects to /setup-profile
- It MUST go to:
   → /login if NOT logged in
   → /dashboard if logged in with profile

2. Setup profile page:
- "Let's Go" button takes too long or does nothing
- It must save instantly and redirect to /dashboard

3. Back button is missing

---

# 🔴 DO NOT PATCH — FIX ROOT CAUSE

---

# ✅ STEP 1 — SINGLE AUTH CHECK (ONLY ONE PLACE)

Implement ONE global auth check using:

onAuthStateChanged(auth, async (user) => {

  setLoading(true)

  if (!user) {
    setUser(null)
    setProfile(null)
    setLoading(false)
    navigate("/login")
    return
  }

  try {
    const snap = await getDoc(doc(db, "users", user.uid))

    if (snap.exists()) {
      setProfile(snap.data())
      navigate("/dashboard")
    } else {
      setProfile(null)
      navigate("/setup-profile")
    }
  } catch (e) {
    console.error("Firestore error:", e)
    navigate("/login")
  }

  setLoading(false)
})

IMPORTANT:
- This must run ONLY ONCE
- REMOVE all other redirect logic from components
- DO NOT redirect anywhere else

---

# ✅ STEP 2 — FIX SETUP PROFILE (INSTANT SUBMIT)

Replace submit function with:

const handleSubmit = async () => {
  if (!name || !grade || !terms) {
    alert("Fill all fields")
    return
  }

  const user = auth.currentUser
  if (!user) {
    alert("User missing")
    return
  }

  try {
    setLoading(true)

    await setDoc(doc(db, "users", user.uid), {
      name,
      grade,
      role: "user",
      createdAt: serverTimestamp()
    })

    navigate("/dashboard")   // 🚀 INSTANT REDIRECT
  } catch (e) {
    console.error("Save error:", e)
    alert("Error saving profile")
  } finally {
    setLoading(false)
  }
}

---

# 🚫 REMOVE THESE BUGS

- Remove ALL extra useEffect redirects
- Remove multiple Firestore calls
- Remove setTimeout delays
- Remove redirect from render()
- Remove duplicate auth checks

---

# 🔙 STEP 3 — ADD BACK BUTTON

Add top navbar:

← Back button:
navigate(-1)

Show on all pages except login

---

# ⚡ STEP 4 — PERFORMANCE FIX

- Only ONE Firestore read after login
- Store profile in state (do not refetch repeatedly)
- Show loading screen ONLY while checking auth
- Prevent infinite re-renders

---

# 🧠 DEBUG (MANDATORY)

Add:

console.log("USER:", user)
console.log("PROFILE:", profile)

---

# 🎯 FINAL RESULT REQUIRED

NOT LOGGED IN:
→ /login

NEW USER:
→ /setup-profile → /dashboard

EXISTING USER:
→ directly /dashboard

NO MORE:
❌ stuck loading
❌ forced /setup-profile
❌ slow button
❌ no redirect

END