Fix two critical system issues in my "Student Hub" React + Firebase app.

DO NOT add features. ONLY fix these problems completely.

---

# 🔴 ISSUE 1 — FIREBASE NOT CONFIGURED (FIX PROPERLY)

Current problem:
- Clicking Register/Google login throws:
  "Firebase is not configured"

---

## REQUIRED FIX:

1. Detect missing Firebase env variables:

Required:
- VITE_FIREBASE_API_KEY
- VITE_FIREBASE_AUTH_DOMAIN
- VITE_FIREBASE_PROJECT_ID
- VITE_FIREBASE_STORAGE_BUCKET
- VITE_FIREBASE_MESSAGING_SENDER_ID
- VITE_FIREBASE_APP_ID

---

2. IF missing:

- DO NOT crash app
- DO NOT throw error

Instead:

- Show UI message:
  "Firebase not configured. Please add credentials."

- Provide a clear instruction panel:
  → "Open Replit → Secrets → Add VITE_FIREBASE_* variables"

---

3. ALSO:

- Log in console:
  "Firebase config missing"

---

4. IMPORTANT:

- Keep login buttons disabled until config exists
- Once env is added → app should work WITHOUT code change

---

# 🔴 ISSUE 2 — /ADMIN REDIRECT BUG (CRITICAL)

Current problem:
- /admin opens briefly then redirects to "/"
- Admin login also redirects to home

---

## REQUIRED FIX (STRICT RULES):

### RULE 1 — ADMIN IS SEPARATE SYSTEM

- /admin MUST NOT use user auth logic
- /admin MUST NOT be affected by Firebase auth state
- /admin MUST NOT redirect to "/"

---

### RULE 2 — ROUTING FIX

Implement proper routing:

/admin → admin login page  
/admin/dashboard → admin dashboard  

---

### RULE 3 — REMOVE BAD REDIRECTS

Find and REMOVE:

- any `navigate("/")` affecting /admin
- any auth guard applied globally
- any useEffect redirect running on /admin

---

### RULE 4 — CONDITIONAL AUTH GUARD

Auth guard must ONLY apply to:

- /dashboard
- /setup-profile
- user-only routes

---

### RULE 5 — ADMIN LOGIN FLOW

When admin logs in:

- DO NOT redirect to "/"
- Redirect ONLY to:

/admin/dashboard

---

### RULE 6 — DEBUG LOGGING (IMPORTANT)

Add:

console.log("Current route:", location.pathname)

Verify:

- /admin stays on admin page
- no auto redirect happens

---

# 🚫 DO NOT

- DO NOT mix admin with user auth
- DO NOT block /admin route
- DO NOT crash if Firebase missing

---

# 🎯 FINAL RESULT

1. If Firebase not set:
   → app shows message instead of crashing

2. /admin works independently:
   → no redirect to home
   → login works correctly

---

END